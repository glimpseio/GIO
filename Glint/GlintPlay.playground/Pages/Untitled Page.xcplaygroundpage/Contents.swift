//: Playground - noun: a place where people can play

import Cocoa
//import Glintlette

var str = "Hello, playground"

let x = 1
x + 2

let y = x.byteSwapped
    .byteSwapped
    .bigEndian
    .littleEndian
    .bigEndian
    .littleEndian
    .distance(to: 5)
    .distance(to: 1)

let z = y - x


Int16(abs(z)).bigEndian.littleEndian

